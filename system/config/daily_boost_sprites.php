<?php

/**
 * Explicit Daily Boost sprite aliases.
 *
 * The default convention is: images/library/<normalized monster name>.gif
 * (or .png/.webp). Add an entry only when a verified client sprite uses a
 * different filename. Never point an entry at a visually similar monster.
 *
 * Example:
 * 'someboss' => 'images/library/some-boss-approved.gif',
 */
return [];
